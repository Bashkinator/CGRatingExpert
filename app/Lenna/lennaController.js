
/** This is module for lennaController. */
var lennaController = angular.module('lennaController', []);

/** This is lennaController - controller for lenna image. */
lennaController.controller('lennaController', function($scope) {
    $scope.lenna = "Hi, I'm Lena Söderberg";
});